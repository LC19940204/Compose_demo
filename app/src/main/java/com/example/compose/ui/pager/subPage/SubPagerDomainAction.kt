package com.example.compose.ui.pager.subPage

import com.example.compose.model.DataStore
import com.example.compose.model.ListItemData
import javax.inject.Inject

class SubPagerDomainAction @Inject constructor(private val dataStore: DataStore) {

}